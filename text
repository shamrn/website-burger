                <ul class="menu-burger">
                 {% for item in menu %}
                {% ifchanged %}
                    <h3 class="menu-title">
                        {{ item.category }}
                     </h3>
                    {% endifchanged %}
                    <li class="menu-item">
                        <img class="burger-img"src="{{item.photo.url}}" alt="">
                        <div class="info-burger">
                            <p class="text-burger name-burger">{{item.name}}</p>
                            <p class="text-burger">{{item.volume}}</p>
                            {% if item.show_ingridients %}
                            <p class="text-burger-ingredients text-burger-animat"><a href="#ingredients-blackout">Состав</a></p>
                            {% endif %}
                            <p class="text-burger-buy text-burger-animat"><a href="#open-buy">Заказать {{item.price}}р</a></p>
                        </div>
                    </li>
                    {% endfor %}
                </ul>